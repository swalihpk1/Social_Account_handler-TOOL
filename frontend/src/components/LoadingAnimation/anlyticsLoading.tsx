
import { InfinitySpin } from 'react-loader-spinner';


const AnalyticsLoading = ({ color}) => {
    return (
        <InfinitySpin
            color={color}
        />
    );
};

export default AnalyticsLoading;